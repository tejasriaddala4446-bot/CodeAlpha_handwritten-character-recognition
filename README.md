# CodeAlpha_handwritten-character-recognition
  Handwritten Character Recognition is a deep learning task used to identify handwritten digits or characters from images. In this task, the **MNIST dataset** is used, which contains grayscale images of handwritten digits (0–9). The images are preprocessed and passed into a **Convolutional Neural Network (CNN)** model. 
